# Evaluation

There are several ways we evaluate the unsoundness checker.

## Performance

Run the following to see the performance compared against real world benchmarks:

```bash
cargo run --release -p evaluation performance
```
